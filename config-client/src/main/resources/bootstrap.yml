spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:2201/
      profile: default
      label: master

server:
  port: 2202

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:2001/eureka
